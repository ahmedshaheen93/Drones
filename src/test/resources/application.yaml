spring:
  jpa:
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
com:
  shaheen:
    scheduler:
      battery: '*/30 * * * * *' # every 10 sec
      status: '*/20 * * * * *' # every 30 sec
